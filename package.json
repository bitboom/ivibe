{
  "name": "ivibe",
  "version": "0.0.1",
  "private": true,
  "description": "iOS-native UX/UI field guide for AI-era app builders.",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs --host 127.0.0.1",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs --host 127.0.0.1"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
